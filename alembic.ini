[alembic]
script_location = alembic
prepend_sys_path = .
version_path_separator = os

# Важно: строка sqlalchemy.url здесь НЕ используется для реальных миграций.
# URL базы задаётся только через переменную окружения DATABASE_URL (см. config.py)
# и подставляется в alembic/env.py перед созданием движка.
# Не копируйте сюда продакшен-DSN — достаточно оставить секцию [alembic] как есть.

[post_write_hooks]
